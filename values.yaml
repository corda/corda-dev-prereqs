# Default values for corda-prereqs.

kafka:
  enabled: true
  auth:
    clientProtocol: tls
    tls:
      type: pem
      autoGenerated: true

postgresql:
  enabled: true
  tls:
    enabled: true
    autoGenerated: true
  volumePermissions:
    enabled: true
  auth:
    database: cordacluster